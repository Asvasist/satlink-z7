# FreeRTOS firmware (Cortex-A9 Core 1)

Stage 4: modem control, TX framing, RX synchronization, FEC, ACM, RPMsg endpoint.
Built with the `rtos-a9` CMake preset. Owned resources: see the address map (`owner: rtos`).
