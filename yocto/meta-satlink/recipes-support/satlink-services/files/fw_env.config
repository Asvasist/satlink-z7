# libubootenv: U-Boot keeps its environment in uboot.env on the FAT boot partition
# (CONFIG_ENV_IS_IN_FAT, CONFIG_ENV_SIZE=0x20000), mounted at /boot/sd.
# @implements SRS-BOOT-002
/boot/sd/uboot.env 0x0000 0x20000
