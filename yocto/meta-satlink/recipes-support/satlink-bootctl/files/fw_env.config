# U-Boot environment for fw_printenv / fw_setenv (libubootenv): the file uboot.env on the FAT boot
# partition, which is mounted at /boot. The size must equal CONFIG_ENV_SIZE in satlink-uboot-ab.cfg.
# @implements SRS-BOOT-003
/boot/uboot.env 0x0000 0x4000
