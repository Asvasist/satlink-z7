# U-Boot options for SatLink-Z7 (merged on top of xilinx_zynq_virt_defconfig).
# Signed FIT images, A/B slots and boot counting follow in Stage 3.
CONFIG_DEFAULT_DEVICE_TREE="zynq-zybo-z7"
CONFIG_BOOTDELAY=2
CONFIG_FIT=y
