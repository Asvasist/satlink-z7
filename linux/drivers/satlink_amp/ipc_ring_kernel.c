// SPDX-License-Identifier: GPL-2.0
/*
 * The shared-memory ring, compiled into the module from the same source the firmware and the
 * host tests use (libs/amp/src/ipc_ring.c). The source is MIT-licensed, which is compatible.
 *
 * @implements SRS-AMP-002
 */
#include "../../../libs/amp/src/ipc_ring.c"
